{
  "name": "chess",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:engine-cs": "dotnet publish engine-cs -c Release && mkdir -p public/dotnet-engine && rm -rf public/dotnet-engine/_framework && cp -r engine-cs/bin/Release/net9.0/publish/wwwroot/_framework public/dotnet-engine/_framework",
    "build": "npm run build:engine-cs && tsc && vite build",
    "preview": "vite preview",
    "bench": "tsx scripts/bench.ts",
    "tournament": "tsx scripts/tournament.ts",
    "perf": "tsx scripts/perf.ts"
  },
  "devDependencies": {
    "tsx": "^4.23.1",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  }
}
